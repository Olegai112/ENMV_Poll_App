# import time
# from src.models.calibrator import Calibrator
#
# calib = Calibrator(parameter="current")
#
# calib.connect()
#
# float_data = calib.send_response(calib.measure_value(0))
#
# print(float_data)
#
# calib.disconnect()

print(bytes([166]))